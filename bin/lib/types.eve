# Eve programming language common data types library.

#required by the C compiler, for allowing many string operations
cimport "clib/strlib.c";

type pint   is ^ int;
type pfloat is ^ float;
type pchar  is ^ char;
type pvoid  is ^void;
